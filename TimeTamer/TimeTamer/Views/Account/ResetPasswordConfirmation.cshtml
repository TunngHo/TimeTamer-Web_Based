@{
    ViewData["Title"] = "Password Reset";
}

<div class="auth-stage">
    <div class="auth-container">
        <div class="auth-header"><span class="auth-badge">TimeTamer</span><h2>Password Reset</h2><p>@(ViewBag.Success ?? "Your password has been reset.")</p></div>
        <a asp-action="Login" class="btn auth-submit">Login</a>
    </div>
</div>
